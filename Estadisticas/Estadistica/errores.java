import java.util.*;
/**
 * trabajara con los movimientos grabados
 * Ira contando cada movimiento y luego sacara porcentajes
 * y hara una grafiquita jeje.
 *
 * @author (your name)
 * @version (a version number or a date)
 */
public class errores
{
    // instance variables - replace the enumeroDeMovimientosample below with your own
    private int numeroDeMovimientos;
    private <HashMap> movimientos<mov><Integer>; //
    
    public int getNumeroDeMovimientos(){
        return numeroDeMovimientos;
    }
    /**
     * Constructor for objects of class errores
     */
    public errores()
    {
        // initialise instance variables
        numeroDeMovimientos = 0;
    }

    /**
     * An enumeroDeMovimientosample of a method - replace this comment with your own
     *
     * @param  y  a sample parameter for a method
     * @return    the sum of numeroDeMovimientos and y
     */
    public void mostrarGraficas()
    {
        /**
         * para calcular el porcentaje
         ( movimientos.get(mov) / numeroDeMovimientos )*100
        */
    }
}
